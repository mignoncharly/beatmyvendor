import type { Metadata } from "next";
import { requireAdmin } from "@/lib/auth";
import { createClient } from "@/lib/supabase/server";
import { AdminFrame } from "@/components/admin-frame";
import s from "../admin.module.css";

export const metadata: Metadata={title:"Deal outcomes"}; export const dynamic="force-dynamic";
export default async function OutcomesPage(){await requireAdmin();const supabase=await createClient();const [{data:outcomes},{data:introductions}]=await Promise.all([supabase.from("deal_outcomes").select("id,introduction_id,outcome,final_annual_price,currency,contract_months,confirmed_at,created_at,public_wins(slug,published_at,buyer_display_name,vendor_display_name)").order("created_at",{ascending:false}).limit(100),supabase.from("introductions").select("id,status,created_at").in("status",["introduced","paid"]).order("created_at",{ascending:false}).limit(100)]);const recorded=new Set(outcomes?.map((o)=>o.introduction_id));const pending=introductions?.filter((i)=>!recorded.has(i.id))||[];return <AdminFrame><div className={s.navSpace}/><div className="dashboard-heading"><div><span className="eyebrow">Marketplace outcomes</span><h1>Deal outcomes</h1></div><span className="badge badge-warning">{pending.length} awaiting</span></div>{pending.length>0&&<div className="notice-success">{pending.length} paid or introduced deal{pending.length===1?" is":"s are"} awaiting buyer outcome confirmation.</div>}<div className={s.tableWrap}><table className={s.table}><thead><tr><th>Recorded</th><th>Outcome</th><th>Final price</th><th>Contract</th><th>Confirmation</th><th>Public win</th></tr></thead><tbody>{outcomes?.map((outcome)=>{const win=Array.isArray(outcome.public_wins)?outcome.public_wins[0]:outcome.public_wins;return <tr key={outcome.id}><td>{new Date(outcome.created_at).toLocaleDateString("en")}</td><td>{outcome.outcome.replaceAll("_"," ")}</td><td>{outcome.final_annual_price&&outcome.currency?new Intl.NumberFormat("en",{style:"currency",currency:outcome.currency,maximumFractionDigits:0}).format(Number(outcome.final_annual_price)):"—"}</td><td>{outcome.contract_months?`${outcome.contract_months} months`:"—"}</td><td>{outcome.confirmed_at?new Date(outcome.confirmed_at).toLocaleDateString("en"):"Pending"}</td><td>{win?.published_at?<a className="text-link" href={`/wins/${win.slug}`}>Published</a>:win?"Draft":"Not created"}</td></tr>})}</tbody></table></div></AdminFrame>}
